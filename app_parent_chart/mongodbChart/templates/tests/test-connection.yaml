apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mongodbChart.fullname" . }}-test-connection"
  labels:
    {{- include "mongodbChart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mongodbChart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

apiVersion: v1
kind: Service
metadata:
  name: {{ include "app_parent_chart.fullname" . }}
  labels:
    {{- include "app_parent_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "app_parent_chart.selectorLabels" . | nindent 4 }}

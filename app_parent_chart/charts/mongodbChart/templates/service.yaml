apiVersion: v1
kind: Service
metadata:
  name: {{ include "mongodbChart.fullname" . }}
  labels:
    {{- include "mongodbChart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mongodbChart.selectorLabels" . | nindent 4 }}

apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontend-nodejs.fullname" . }}-test-connection"
  labels:
    {{- include "frontend-nodejs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontend-nodejs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

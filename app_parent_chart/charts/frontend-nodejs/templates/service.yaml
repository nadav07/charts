apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontend-nodejs.fullname" . }}
  labels:
    {{- include "frontend-nodejs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontend-nodejs.selectorLabels" . | nindent 4 }}

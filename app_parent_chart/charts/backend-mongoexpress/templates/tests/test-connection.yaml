apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend-mongoexpress.fullname" . }}-test-connection"
  labels:
    {{- include "backend-mongoexpress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend-mongoexpress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
